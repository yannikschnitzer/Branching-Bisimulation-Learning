MODULE main
    VAR a: --65536..65535;
    VAR r: --65536..65535;
    VAR n: --65536..65535;

    INIT a = 0 & r = 0 & n >= 15;

    TRANS (next(a) = a & next(r) = r & next(n) = n)
        | (next(a) = (a = 1 ? 0 : 1) & next(r) = (r = 1 ? 0 : 1) & next(n) = n)
        ;