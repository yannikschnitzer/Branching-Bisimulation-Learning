MODULE main
    VAR w: --65536..65535;
    VAR g: --65536..65535;

    TRANS (next(w) = 0 & next(g) = (g >= 1 ? 0 : g))
        | (next(w) = 1 & next(g) = (g >= 1 ? 0 : g))
        ;

    LTLSPEC F(G(w != 1))