MODULE main
    VAR x : --65536..65535;
    VAR y : --65536..65535;
    
    DEFINE terminated := x <= 0;

    TRANS ((terminated           & next(x) = x)
        | (!terminated          & next(x) = x - 1)
        | (!terminated & x >= y & next(x) = x + 1)
        | (!terminated & x <  y & next(x) = x - 1))
        & next(y) = y
        ;

    