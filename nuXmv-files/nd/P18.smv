MODULE main
    VAR x: integer;

    TRANS (next(x) = x + 1)
        | (next(x) = (x > 2 ? 2 : x + 1))
        ;
    
    --LTLSPEC F(x>= 1)
    --LTLSPEC x <= 1 -> G(x <= 1)
    --LTLSPEC G(F(x >= 1))
    LTLSPEC F G (x >= 1)

